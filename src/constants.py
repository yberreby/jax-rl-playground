# Initialization constants
DEFAULT_SPARSITY = 0.5
DEFAULT_SPARSE_INIT_SPARSITY = 0.9  # Different from policy sparsity
INITIAL_LOG_STD = 0.0  # exp(0.0) = 1.0 for more initial exploration

# Normalization constants
DEFAULT_EPSILON = 1e-8
DEFAULT_CLIP_VALUE = 5.0

# Architecture defaults
DEFAULT_HIDDEN_DIM = 64

# Test defaults
DEFAULT_OBS_DIM = 4
DEFAULT_ACTION_DIM = 2
DEFAULT_SEED = 42
